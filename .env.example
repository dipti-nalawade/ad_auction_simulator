# Copy this file to .env and fill in your values.
# Export these before starting the PHP dev server:
#   export $(cat .env | xargs) && php -S localhost:8080 -t public

DB_HOST=localhost
DB_NAME=ad_auction_simulator
DB_USER=your_db_user
DB_PASS=your_db_password
